# ✈️ Полётный Симулятор — Flight Physics Simulator

Браузерный 3D-симулятор полёта с реалистичной аэродинамической моделью. Построен на **Three.js** без сборщиков — чистый HTML/CSS/JS.

![Three.js](https://img.shields.io/badge/Three.js-r128-blue) ![License](https://img.shields.io/badge/license-MIT-green) ![Status](https://img.shields.io/badge/status-playable-brightgreen)

---

## 🎮 Демо

Запустите локальный сервер и откройте в браузере:

```bash
python3 -m http.server 8888
```

Перейдите на [http://localhost:8888](http://localhost:8888)

---

## 🛩️ Доступные самолёты

| Самолёт | Тип | Макс. скорость | Потолок |
|---------|-----|----------------|---------|
| **Cessna 172** | Лёгкий учебный | 226 км/ч | 4 100 м |
| **F-16 Falcon** | Истребитель | 2 120 км/ч | 15 000 м |
| **Boeing 737** | Авиалайнер | 876 км/ч | 12 500 м |

Каждый самолёт имеет уникальные лётные характеристики: массу, площадь крыла, тягу, скорость сваливания, коэффициенты подъёмной силы и сопротивления.

---

## 🎛️ Управление

| Клавиша | Действие |
|---------|----------|
| `W` / `S` | Тангаж (вверх/вниз) |
| `A` / `D` | Крен (влево/вправо) |
| `Q` / `E` | Рыскание (влево/вправо) |
| `Shift` / `Ctrl` | Тяга (увеличить/уменьшить) |
| `F` | Закрылки (0° → 10° → 20° → 30°) |
| `G` | Шасси (выпуск/уборка) |
| `B` | Воздушный тормоз |
| `V` | Смена камеры (кабина / преследование / внешняя) |
| `R` | Сброс позиции |
| `P` | Пауза |

---

## ⚙️ Физическая модель

Симулятор реализует полноценную аэродинамическую модель:

- **Подъёмная сила** — рассчитывается через коэффициент CL в зависимости от угла атаки α:

  `L = ½ · ρ · v² · S · CL(α)`

- **Сопротивление** — паразитное (CD₀), индуктивное и волновое (при околозвуковых скоростях):

  `D = ½ · ρ · v² · S · CD`

- **Атмосфера** — барометрическая модель плотности воздуха:

  `ρ(h) = ρ₀ · e^(−h / H)`, где H = 8500 м — масштабная высота

- **Сваливание** — моделируется при превышении критического угла атаки
- **G-нагрузка** — рассчитывается в реальном времени
- **Число Маха** — трансзвуковое увеличение сопротивления
- **6 степеней свободы** — ориентация через кватернионы, угловые скорости по трём осям

---

## 🏗️ Структура проекта

```
├── index.html      # Главная страница: стартовый экран, HUD, выбор самолёта
├── styles.css      # Стилизация: HUD, индикаторы, загрузка, адаптивность
├── physics.js      # Аэродинамический движок (FlightPhysics)
├── aircraft.js     # 3D-модели самолётов из примитивов Three.js (AircraftModel)
├── world.js        # Генерация мира: ландшафт, небо, облака, ВПП, здания (World)
├── hud.js          # Обновление HUD: приборы, авиагоризонт, компас (HUD)
├── main.js         # Игровой цикл, ввод, камера, инициализация
└── README.md
```

---

## 🗺️ Мир

- Процедурно-генерируемый **рельеф** с горами и равнинами
- **Взлётно-посадочная полоса** с разметкой и огнями
- **Шейдерное небо** с градиентом и атмосферой
- **Облака** из частиц
- **Деревья и здания** для ориентации

---

## 📊 HUD-интерфейс

- Скорость (км/ч), высота (м), вертикальная скорость (м/с)
- Курс (°) с компасной лентой
- Авиагоризонт с тангажными метками и указателем крена
- Индикатор тяги, G-нагрузка, угол атаки
- Статус: закрылки, шасси, воздушный тормоз
- Предупреждения: сваливание, превышение скорости, близость земли

---

## 🚀 Запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/<ваш-логин>/flight-simulator.git
   cd flight-simulator
   ```

2. Запустите любой HTTP-сервер:
   ```bash
   # Python
   python3 -m http.server 8888

   # Node.js
   npx serve -p 8888

   # VS Code — расширение Live Server
   ```

3. Откройте [http://localhost:8888](http://localhost:8888) в браузере.

> **Зависимости**: отсутствуют. Three.js r128 загружается с CDN.

---

## 🛠️ Технологии

- [Three.js](https://threejs.org/) r128 — 3D-рендеринг
- Vanilla JavaScript — без фреймворков и сборщиков
- CSS3 — HUD-интерфейс, анимации
- HTML5 Canvas — компасная лента

---

## 📝 Лицензия

MIT
